{
  "name": "zap-jobs-be",
  "version": "1.0.0",
  "description": "A job portal which connects freelancers and employers - Intuit Assessment Project",
  "main": "index.ts",
  "scripts": {
    "install": "cd frontend && yarn install && cd ../backend && yarn install && cd ..",
    "build": "yarn build:frontend && yarn move-build-to-backend",
    "build:frontend": "cd frontend && yarn build",
    "move-build-to-backend": "rm -rf backend/build && mv frontend/build backend/build"
  },
  "keywords": [],
  "author": "Rakshith Sindhiya R",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "@graphql-yoga/node": "^3.9.1",
     "graphql": "^16.9.0",
     "dotenv": "^16.4.3",
     "mongoose": "^6.0.14"
  },
  "devDependencies": {
    "@types/node": "^22.2.0",
    "@types/express": "^4.17.21",
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2"
  }
}
